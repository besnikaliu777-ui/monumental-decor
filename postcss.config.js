/**
 * PostCSS configuration. Tailwind CSS and Autoprefixer are loaded
 * automatically. No other PostCSS plugins are required for this
 * project, but you can extend this configuration to include
 * additional transformations if desired.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};